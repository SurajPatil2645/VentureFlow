OpenAI API Configuration
Get your API key from: https://platform.openai.com/api-keys
# This is used for AI-powered company enrichment and data extraction
OPENAI_API_KEY="your_openai_api_key_here"

# Optional: Specify a different OpenAI model (defaults to gpt-4-turbo)
# OPENAI_MODEL=gpt-4-turbo

# Rate Limiting Configuration (optional)
# Maximum number of enrichment requests per minute (default: 10)
RATE_LIMIT_PER_MINUTE=10

# Cache Configuration (optional)
# Enable persistent caching (default: true)
ENABLE_CACHE=true

# Cache TTL in milliseconds (default: 7 days = 604800000ms)
CACHE_TTL=604800000
